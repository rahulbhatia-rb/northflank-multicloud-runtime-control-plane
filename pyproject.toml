[project]
name = "northflank-multicloud-runtime-control-plane"
version = "0.1.0"
description = "Executable production-readiness controls for a multi-cloud Kubernetes platform"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
